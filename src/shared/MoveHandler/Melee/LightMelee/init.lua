return {
    damage = 10,
    criticalChance = 20, --chance of doing critical damage
    criticalDamageMultiplier = 2, --damage multiplier for critical hit
    normalHitCooldown = 0.25, --cooldown for every hit prior to the last one
    finalHitCooldown = 0.5, --cooldown after every attack animation has been cycled through
    hitboxSize = Vector3.new(5, 5, 5),
}